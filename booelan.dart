void main() {
  // untuk mempresentasikan nilai boolean, Dart memiliki tipe bernama bool
  var fullName = 'Adam Ray Wibowo';
  var hitPoints = 0;
  var unicorn;
  var iMeantToDoThis = 00;
  // cek apakah variabel tesebut memiliki nilai?
  print(fullName.isEmpty);

  // cek apakah nilai yang ada di hitPoints kurang dari atau sama dengan 0?
  print(hitPoints <= 0);

  // cek for null
  print(unicorn == null);

  // cek NaN
  print(iMeantToDoThis.isNaN);
}
